/** @type {import('next').NextConfig} */
const nextConfig = {
    transpilePackages: ['gsap'],
    /* Your Next.js config */
};

module.exports = nextConfig
module.exports = {
    reactStrictMode: false,
  };
 
